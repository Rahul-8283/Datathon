{
  "appsail": [
    {
      "source": ".",
      "name": "ksp-backend",
      "command": "uvicorn main:app --host 0.0.0.0 --port $X_ZOHO_CATALYST_LISTEN_PORT"
    },
    {
      "source": ".",
      "name": "ksp-celery-worker",
      "command": "celery -A core.celery_app worker --loglevel=info --pool=solo & python -m http.server $X_ZOHO_CATALYST_LISTEN_PORT"
    }
  ]
}
